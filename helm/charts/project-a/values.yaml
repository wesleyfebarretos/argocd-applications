golang-api:
  enabled: true
  fullnameOverride: golang-api

cnpg:
  cluster:
    instances: 1
    major: 18
    storage:
      size: 1Gi
    resources:
      requests:
        memory: "32Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
